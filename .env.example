# URL do backend (server/). Deixe vazio para rodar o app 100% em memória,
# sem persistência (comportamento antigo, sem backend).
#
# Local:
# VITE_API_URL=http://localhost:4000
#
# Produção (Vercel), aponte para o backend do Render, SEM barra no final:
# VITE_API_URL=https://seu-backend.onrender.com
VITE_API_URL=
