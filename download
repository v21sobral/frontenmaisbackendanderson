# Dependências
node_modules/
server/node_modules/

# Build
dist/
server/dist/

# Variáveis de ambiente (nunca commitar segredos/URLs locais)
.env
.env.local
server/.env

# "Banco de dados" do backend gerado em runtime a partir do seed.json —
# não deve ser commitado, senão o repo fica com dados de teste desatualizados
server/data/db.json

# Editor / SO
.DS_Store
